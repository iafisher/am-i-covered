To run, install requirements, create the database and start the server:

```
$ pip3 install -r requirements.txt
$ python3 createdb.py
$ FLASK_APP=server.py flask run
```

Then visit `localhost:5000` in a browser.
